[build]
  publish = "public"
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.140.2"  # Použij aktuální verzi Hugo
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_VERSION = "0.140.2"
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL"

[context.branch-deploy]
  command = "hugo --gc --minify -b $DEPLOY_PRIME_URL"

[[redirects]]
  from = "/netlify.toml"
  to = "/404"
  status = 404

# Pokud chceš vlastní doménu, přidej:
# [[redirects]]
#   from = "https://www.tvoje-domena.cz/*"
#   to = "https://tvoje-domena.cz/:splat"
#   status = 301
#   force = true
